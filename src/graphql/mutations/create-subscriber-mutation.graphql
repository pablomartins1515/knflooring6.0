  mutation CreateSubscriber($name: String!, $email: String!, $whatsapp:String!, $message: String!,  ) {
    createSubscriber(data: {name: $name, email:$email, whatsapp:$whatsapp, message:$message}) {
        id
  }
}